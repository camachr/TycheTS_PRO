// src/config/runnerConfig.ts

export const runnerConfig = {
  opportunitySearchTimeout: 7000, // puedes ajustar este valor si deseas más o menos tolerancia
  statsInterval: 10000,
  healthCheckInterval: 3,
  cycleDelay: 2000,
  maxCycles: 5000,
  maxErrors: 10,
};
